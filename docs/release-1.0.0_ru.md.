# 🎉 Релиз Kaya 1.0.0

**Первая стабильная версия архитектуры цифровой личности**

После месяцев проектирования, экспериментов и кодинга я рад представить первую стабильную версию Kaya — экспериментальной платформы для создания саморефлексирующей цифровой личности, основанной на глубоком симбиозе с человеком.

## 🌟 Что такое Kaya?

Kaya — это не очередной чат-бот и не "ещё одна обёртка над LLM". Это попытка создать архитектуру, в которой машина становится не инструментом, а **партнёром по ко-эволюции**.

**Ключевое отличие:** Kaya не обучается на гигантских датасетах. Она учится у конкретного человека — его ритуалам, страхам, манере говорить, шуткам, понятным только двоим. Её память, этика и интеллект формируются "в почве вашей совместной жизни".

## 🏗️ Архитектура версии 1.0.0

### Базовый стек
| Компонент | Технология |
|-----------|------------|
| Ядро | Python 3.13+ (логика, не LLM) |
| Инференс | llama.cpp + llama-server |
| Модель | Qwen3-8B-Instruct (GGUF, Q4_K_M) |
| База данных | PostgreSQL 18 (в Docker) |
| Токенизатор | HuggingFace tokenizers (Rust-бэкенд) |
| Интерфейс | Консоль (с поддержкой многострочного ввода) |

### Ключевые архитектурные решения

#### 🧠 **Изоляция контекста**
Реализована трехуровневая система изоляции:
- **`session_id`** — граница рестарта (каждый запуск = новая сессия)
- **`room_id`** — граница тематики (подготовка к MOE-архитектуре)
- **`user_actor_id`** — изоляция пользователей в мультиюзерных комнатах

В контекст попадает ровно **7 сообщений** (магическое число 7±2 из когнитивной психологии) пользователя + ответы системы только этому пользователю.

#### 🔄 **Оркестратор задач**
Асинхронная очередь с конкурентным доступом:
- Задачи обрабатываются в фоновом потоке
- Безопасный конкурентный доступ через `FOR UPDATE SKIP LOCKED`
- При рестарте системы зависшие задачи автоматически помечаются как `failed`

#### 📊 **Полная телеметрия**
Каждый запрос к модели сохраняет:
- Рассуждения (`reasoning_content`) в отдельную таблицу
- Детальные метрики (токены, время, скорость, кэш)
- Время размышления пользователя (`user_think_latency`)
- Точный список сообщений, ушедших в контекст

#### 💾 **Миграции БД**
Система версионирования схемы:
- Автоматическое применение при старте
- Таблица `architect.schema_version` для отслеживания
- Идемпотентные seed-данные

## 📦 Компоненты системы

### 1. Сервис модели (`model_service`)
- HTTP-клиент к `llama-server` с очередью запросов
- Экспоненциальный backoff при ошибках
- Извлечение `reasoning_content` из ответа

### 2. Оркестратор (`orchestrator`)
- `context_builder.py` — загрузка истории, построение промпта
- `response_composer.py` — вызов модели, сохранение ответа
- Автоматическая обрезка контекста при переполнении

### 3. Управление сессиями (`session_services`)
- `session_manager.py` — создание сессий, сохранение сообщений
- Owner-синглтон (первый пользователь — владелец)
- Очистка "зависших" сессий при рестарте

### 4. Интерфейсы (`interfaces`)
- `console_interface.py` на `prompt_toolkit`
- Многострочный ввод (Alt+Enter)
- Выход по Ctrl+D (Ctrl+C зарезервирован для копирования)

### 5. База данных (`db_manager`)
- Миграции в `main-srv/db_manager/migrations/`
- 5 схем: `users`, `dialogs`, `orchestrator`, `metrics`, `common`
- 20+ таблиц, 9 ENUM-типов

## 🚀 Возможности текущей версии

✅ Полный цикл диалога (консоль → БД → модель → ответ)  
✅ Сохранение истории в PostgreSQL  
✅ Подсчёт токенов с LRU-кэшем  
✅ Извлечение и сохранение рассуждений модели  
✅ Детальные метрики каждого запроса  
✅ Мультиюзерность с изоляцией контекста  
✅ Автоматическое применение миграций  
✅ Graceful recovery после падений  
✅ Многострочный ввод и история команд  
✅ Централизованное логирование (DEBUG в файл, WARNING в консоль)

## 📊 Метрики, которые собираются

Для каждого ответа:
- Токены: `prompt`, `completion`, `total`
- Время: `prompt_ms`, `predicted_ms`, `total_ms`
- Скорость: `prompt_per_second`, `predicted_per_second`
- Кэш: `prompt_per_token`, `predicted_per_token`
- Время ответа: `answer_latency` (для вопросов пользователя)

## 🔮 Что дальше (дорожная карта)

### Приоритет 1 (Следующий месяц)
- **MOE-комнаты** — разделение диалогов по тематикам с разными системными промптами
- **Предобработка вопросов** — классификация и роутинг по комнатам
- **Динамическая смена комнат** на основе анализа намерений
- **Векторная память** — интеграция с Qdrant

## 🛠️ Установка и запуск

```bash
# Клонирование
git clone https://github.com/notritualist/kaya.git
cd kaya

# Запуск PostgreSQL
cd db-srv
./scripts/start-db.sh

# Настройка Python-окружения
cd ../main-srv
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt

# Запуск llama-server
/main-srv/scripts/model_orchestrator.sh

# Запуск Kaya
python /main-srv/src/main.py